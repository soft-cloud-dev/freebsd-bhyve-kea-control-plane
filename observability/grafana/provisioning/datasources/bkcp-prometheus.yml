apiVersion: 1

prune: true

datasources:
  - name: BKCP Prometheus
    uid: bkcp-prometheus
    type: prometheus
    access: proxy
    url: http://127.0.0.1:9090
    isDefault: true
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: 15s
