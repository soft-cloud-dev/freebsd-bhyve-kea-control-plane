apiVersion: 1

providers:
  - name: FreeBSD control plane
    orgId: 1
    folder: FreeBSD Control Plane
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /usr/local/etc/grafana/provisioning/dashboards/json
